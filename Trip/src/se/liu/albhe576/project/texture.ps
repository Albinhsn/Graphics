#version 400

in vec2 texCoord;

out vec4 outputColor;

uniform sampler2D shaderTexture;

void main(void)
{
	vec4 color = texture(shaderTexture, texCoord);
	if(color.a < 0.1){
		discard;
	}
	outputColor = color;
}